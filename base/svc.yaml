apiVersion: v1
kind: Service
metadata:
  labels:
    app: mongodb-mongodb
    release: mongodb
  name: mongodb-mongodb
spec:
  ports:
  - name: mongodb
    port: 27017
    targetPort: mongodb
  selector:
    app: mongodb-mongodb
